#include <stdio.h>

/**
 * main - print single digit number of base 10 from 0
 *
 * Return: zero
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
