#include "main.h"
#include <stdio.h>

/**
 * _isupper - check if character is uppercase
 * @c: character to be checked
 * Return: Always 0
 */

int _isupper(int c)
{
	if (i >= 'A'; i <= 'Z'; i++)
	{
		return (1);
	}
	return (0);
}
